# smart-fan
The Smart Fan is a temperature-controlled cooling system built using an Arduino microcontroller. The system automatically turns the fan ON when the ambient temperature exceeds 23 °C and turns it OFF when the temperature falls below the threshold with hysteresis.
